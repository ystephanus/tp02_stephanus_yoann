import { ErrorsDirective } from './errors.directive';

describe('ErrorsDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorsDirective();
    expect(directive).toBeTruthy();
  });
});
